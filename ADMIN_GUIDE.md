# 管理员使用说明

后台地址 `/admin`。当前是单管理员测试版，不是完整用户账号系统。

## 首次启用：部署负责人本人操作

在 Render 此服务的 Environment 设置 `ADMIN_USERNAME`（默认 admin）、`ADMIN_PASSWORD`（至少16位独立随机密码）、`NODE_ENV=production`。保存并重新部署，再到 `/admin` 登录。不要将密码发到聊天、提交 GitHub，也不要使用测试文件中的密码。

高级配置支持 `ADMIN_PASSWORD_HASH`（scrypt，32位十六进制 salt + 冒号 + 128位十六进制摘要），其优先级高于 ADMIN_PASSWORD。更换密码时避免保留旧 HASH。没有有效配置时登录关闭，不存在默认密码或公开管理员注册。

会话1小时到期；重启全部退出。输错5次需等15分钟；按连接IP限速，代理后的多人可能共用额度。

## 日常管理

1. Helper审核：核实信息后审核通过，填原因并确认。暂停接单会取消匹配资格；撤销审核不会恢复已暂停的 Helper。
2. 需求管理：不适合的需求下架；无效的需求关闭。终态不可直接恢复。管理员不能代替外部求助者认领或跳到 OPEN。
3. 操作记录：页面显示最近100条；存储包含时间、操作者、目标、原因和前后状态。原因中不要输入证件、联系方式等敏感信息。
4. 使用公共设备后退出登录。

平台审核通过仅是匹配资格，不是身份证认证、背景调查或保险证明。演示 Helper 不是真实服务人员。

## 用户角色与运营边界

产品设计允许一个成员同时求助和提供帮助，管理员是附加权限。但当前只有独立的需求和 Helper 表单，尚未关联到已认证成员ID。管理员登录不等于成员登录；同一账号双角色、个人任务管理和禁止自接单待下一阶段实现/验收。

正式运营阻塞项：

- Render尚未确认持久磁盘：JSON和审核记录可能随重启/重新部署丢失。本次未新增收费服务。
- 成员认证、所有权校验、真正签名的一次性认领链接未实现。旧 claim 接口仅检查显式确认及方法名称，不构成身份验证。
- 公开提交缺少完整反滥用保护；原生任务描述尚未全面过滤个人信息。
- 支付、退款、争议、保险、背景调查及高风险任务限制尚未形成可执行闭环。

本版用于管理员验收和受控演示，不应直接开放真实任务。

## 验证

`npm test` 运行模型、Discovery和管理员权限/审核/存储测试。
`npm run smoke` 启动真实应用及内部服务，覆盖 Discovery、后台登录、审核、操作记录、关闭需求和退出登录。使用临时数据库与测试专用密码，不改线上数据。
