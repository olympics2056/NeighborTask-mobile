{
  "name": "neighbortask-mobile",
  "version": "3.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node app-server.mjs",
    "dev": "NODE_ENV=development node app-server.mjs",
    "test": "node --test tests/*.test.mjs",
    "smoke": "node tests/e2e-smoke.mjs",
    "check": "node --check app-server.mjs && node --check agent-core.mjs && node --check workflow-server.mjs && node --test tests/*.test.mjs"
  },
  "engines": { "node": ">=20" },
  "dependencies": {
    "@openai/agents": "latest",
    "zod": "^4.0.0"
  }
}
